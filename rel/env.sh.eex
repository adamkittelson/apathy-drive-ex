#!/bin/sh

# host = eval "ip addr show eno1 | grep -Po 'inet \K[\d.]+'"

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=apotheosis@127.0.0.1
#export RELEASE_COOKIE=$(cat $RELEASE_VM_ARGS | grep -Po '-setcookie \K(\w+)')
#echo $RELEASE_COOKIE

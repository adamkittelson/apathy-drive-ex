defmodule ApathyDrive.Exits.Door do
  #use ApathyDrive.Exits.Doors

  # def look(current_room, mobile, room_exit) do
  #   if open?(current_room, room_exit) do
  #     #super(current_room, mobile, room_exit)
  #   else
  #     Mobile.send_scroll(mobile, "<p>The #{name} is closed in that direction!</p>")
  #   end
  # end

end
